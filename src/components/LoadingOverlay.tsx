import React from 'react';

const LoadingOverlay = () => {
  return (
    <div className="loading-overlay">
      <div className="spinner">
        {/* Inclua aqui o seu ícone de carregamento */}
      </div>
    </div>
  );
};

export default LoadingOverlay;